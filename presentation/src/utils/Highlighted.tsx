import React from 'react';
import styled from 'styled-components';
import { Color } from './Colors';

const Highlighted = styled.span`
  color: ${Color.FirebaseLight};
`;

export default Highlighted;
